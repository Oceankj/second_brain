{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:personal-agent-memory:schema:ingest_turn:input",
  "title": "ingest_turn input",
  "type": "object",
  "additionalProperties": false,
  "required": ["token", "user_input", "assistant_output", "metadata"],
  "properties": {
    "token": {
      "type": "string",
      "minLength": 1,
      "description": "API token used to authenticate the caller and resolve user identity."
    },
    "user_input": {
      "type": "string",
      "minLength": 1
    },
    "assistant_output": {
      "type": "string",
      "minLength": 1
    },
    "metadata": {
      "type": "object",
      "additionalProperties": true,
      "required": ["timestamp", "source"],
      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time"
        },
        "source": {
          "type": "string",
          "minLength": 1,
          "description": "資料來源，例如 codex、dify、claude-desktop。"
        },
        "app": {
          "type": "string"
        },
        "user_id": {
          "type": "string",
          "description": "Optional caller hint retained in metadata; persisted user identity is resolved from token."
        },
        "session_id": {
          "type": "string"
        },
        "conversation_id": {
          "type": "string"
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "ingest_reason": {
          "$ref": "#/$defs/ingest_reason"
        },
        "skip_memory": {
          "type": "boolean",
          "default": false,
          "description": "When true, ingest_turn returns skipped and does not write durable memory."
        }
      }
    }
  },
  "$defs": {
    "ingest_reason": {
      "type": "string",
      "description": "Explicit caller policy signal explaining why this interaction should become durable memory.",
      "enum": [
        "task_completed",
        "explicit_memory_request",
        "user_preference",
        "stable_fact",
        "personal_insight",
        "decision",
        "stable_artifact",
        "manual_import"
      ]
    }
  }
}
